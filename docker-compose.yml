name: soofi-trainer

include:
  - path: compose/admin.yml
    project_directory: .
  - path: compose/knowledge.yml
    project_directory: .
  - path: compose/training.yml
    project_directory: .
  - path: compose/interaction.yml
    project_directory: .
  - path: compose/tools.yml
    project_directory: .
  - path: compose/aas.yml
    project_directory: .
  - path: compose/edc.yml
    project_directory: .

# ===========================================
# Networks
# ===========================================
networks:
  soofi-network:
    driver: bridge
    name: soofi-network
  soofi-training-network:
    driver: bridge
    name: soofi-training-network
  soofi-admin-network:
    driver: bridge
    name: soofi-admin-network
  soofi-edc-network:
    driver: bridge
    name: soofi-edc-network
  soofi-aas-network:
    driver: bridge
    name: soofi-aas-network

